PROGRAM NUMERO_PRIMO2;
USES CRT;
VAR
  I,N: WORD;
BEGIN
  CLRSCR;
  WRITE('N= ');
  READLN(N);
  IF N=1 THEN I:=0
  ELSE I:=1;
  REPEAT
    INC(I)
  UNTIL N MOD I =0;
  IF I = N THEN
  	 WRITELN('ES PRIMO')
  ELSE
    WRITELN('NO ES PRIMO');
  REPEAT UNTIL KEYPRESSED;
END.